import { DefaultTheme } from "react-native-paper";

const theme = {
  ...DefaultTheme,
  roundness: 2,
  colors: {
    ...DefaultTheme.colors,
    primary: "#000000",
    secondary: "#FFFFFF",
    accent: "#FFFFFF"
  }
};

export default theme;
